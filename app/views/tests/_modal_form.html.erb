<div class="modal-content">
<%= simple_form_for @test, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h4 class="modal-title" id="myModalLabel">Добавить тест</h4>
  </div>
  <div class="modal-body" id="new-form">
    <%= f.input :title, label: 'Название' %>
    <%= f.hidden_field :test_group_id %>
    <%= f.hidden_field :algorithm %>
  </div>
  <div class="modal-footer">
    <button type="button" class="btn btn-default" data-dismiss="modal">Отмена</button>
    <%= f.button :submit, 'Добавить', :class => 'btn btn-primary' %>
  </div>
<% end %>
</div>